// Conteo de caracteres: Dada una cadena de texto y luego muestra el número de caracteres que contiene esa cadena.

let cadena = "Betancourt";

function contarCaracteres(cadena) {
  return cadena.length;
}

console.log(`La cadena ${cadena} contiene ${contarCaracteres(cadena)} caracteres`); 
//Muestra: La cadena Betancourt contiene 10 caracteres

